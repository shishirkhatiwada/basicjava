// Inheritance allows one class (subclass or derived class) to inherit the attributes and behaviors of another class (superclass or base class).
// It promotes code reusability and the creation of specialized classes.


class Animal {
    void eat() {
        // Implementation for eating
    }
}

class Dog extends Animal {
    void bark() {
        // Implementation for barking
    }
}
